<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.landingzone.dreamweb.dao.ProductDao">

    <select id="listApplication"
            resultType="String">
        select distinct application from product where productid = #{productId}
    </select>

    <select id="getPortfolioId" 
            resultType="String">
        select portfolioid from userproduct where productid = #{productId} and username = #{userName}
    </select>

    <select id="listScenes"
            resultType="String"
            parameterType="String">
        select distinct scenes from product where application = #{application} and productid = #{productId}
    </select>

    <select id="getProductVersionId"
            resultType="String">
        select productversionid from product where application = #{application} and scenes = #{scene} and productid = #{productId};
    </select>

    <select id="getProductName"
            resultType="String">
        select distinct productname from product where productid = #{productId};
    </select>

    <select id="listProduct" resultType="cc.landingzone.dreamweb.model.Product" parameterType="map">
        select * from product order by id desc
        <if test="page != null">
            limit #{page.start},#{page.limit}
        </if>
    </select>

    <select id="getProductTotal" resultType="int" parameterType="map">
        select count(*) from product
    </select>    
    
    <select id="getProductByProductId" resultType="cc.landingzone.dreamweb.model.Product">
        select *
        from product
        where productid = #{productId}
    </select>

    <select id="getProductById" resultType="cc.landingzone.dreamweb.model.Product">
        select *
        from product
        where id = #{id}
    </select>

    <insert id="saveProduct"
            parameterType="cc.landingzone.dreamweb.model.Product">
        insert into product
        (application,
        scenes,
        productid,
        productname,
        productversionid,
        gmt_create)
        values (#{application},
                #{scenes},
                #{productId},
                #{productName},
                #{productVersionId},
                now())
    </insert>

    <update id="updateProduct" parameterType="cc.landingzone.dreamweb.model.Product">
        update product
        set application              = #{application},
            scenes                   = #{scenes},
            productid                = #{productId},
            productname              = #{productName},
            productversionid         = #{productVersionId}
        where id = #{id}
    </update>

    <delete id="deleteProduct" parameterType="int">
        delete
        from product
        where id = #{id}
    </delete>

</mapper>